!!!
html(lang="en")
  head
    title Express Demo with Hull Auth

    script(src="//ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js")
    script(src="//hull-js.s3.amazonaws.com/develop/hull.js")
    link(rel="stylesheet")(href="http://hullstrap.s3.amazonaws.com/stylesheets/hullstrap.css")
    script
      Hull.on('hull.authComplete', function() {
        _.delay(function() { document.location.reload() }, 1000);
      });
      Hull.init(!{JSON.stringify(config)});
  body
    div.container
      div.hero-unit
        h1 Authenticate with Hull
      h3 Hull Identity widget
      div(data-hull-widget="identity@hull")

      h3 User logged in ?
      pre
        !{JSON.stringify(current_user)}

    div(data-hull-widget="footer@hull")
